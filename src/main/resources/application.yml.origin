spring:
  datasource:
    driverClassName:
    url:
    username:
    password:
  sql:
    init:
      mode:
  web:
    resources:
      add-mappings:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri:
server:
  port:
  ssl:
    key-store-type:
    key-store:
    key-store-password: